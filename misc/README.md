# Maintainer references

* [Python module execution and data-flow audit](python_module_audit.md) records the role, imports, invocation contract, data/checkpoint requirements, outputs, execution mode, and downstream consumers for every tracked Python file under `src/`.
